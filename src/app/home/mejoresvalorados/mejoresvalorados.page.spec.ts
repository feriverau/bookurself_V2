import { ComponentFixture, TestBed } from '@angular/core/testing';
import { MejoresvaloradosPage } from './mejoresvalorados.page';

describe('MejoresvaloradosPage', () => {
  let component: MejoresvaloradosPage;
  let fixture: ComponentFixture<MejoresvaloradosPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(MejoresvaloradosPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
